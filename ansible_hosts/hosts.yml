all:
  vars:
    domain: test.ansible.no
    fileshare_mount_point: ""
    router_default_gw: 172.17.0.8 

    dns_servers:
      - "{{ router_default_gw }}"
      - 8.8.8.8
      - 8.8.4.4

  children:
    router:
      children:
        virtual:
          hosts:
            kb0:

    router_clients:
      children:
        baremetal:
          children:
            cdh_utility:
              hosts:
                kb1:

            cdh_gateway:
              hosts:
                kb2:

            cdh_data:
              hosts:
                kb3:

        #virtual:
        #  hosts:
        #    kb2:
