all:
  vars:
    domain: test.ansible.no
    fileshare_mount_point: ""
    router_default_gw: 192.168.43.95 

    dns_servers:
      - "{{ router_default_gw }}"
      - 8.8.8.8
      - 8.8.4.4

  children:
    router:
      children:
        virtual:
          hosts:
            kb0:

    router_clients:
      children:
        baremetal:
          children:
            cdh_utility:
              hosts:
                kb1:

            cdh_gateway:
              hosts:
                kb2:

            #cdh_data:
            #  hosts:
            #    kb2:

        #virtual:
        #  hosts:
        #    kb2:
